name: Publish

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write # required for NuGet trusted publishing (OIDC)
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
          fetch-depth: 0

      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Extract version from csproj
        id: version
        run: |
          VERSION=$(grep -oP '(?<=<Version>)[^<]+' ServiceBusConsole.csproj)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - name: Check if tag already exists
        id: tag_check
        run: |
          if git ls-remote --tags origin "refs/tags/v${{ steps.version.outputs.VERSION }}" | grep -q .; then
            echo "EXISTS=true" >> $GITHUB_OUTPUT
          else
            echo "EXISTS=false" >> $GITHUB_OUTPUT
          fi

      - name: Tag release
        if: steps.tag_check.outputs.EXISTS == 'false'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag v${{ steps.version.outputs.VERSION }}
          git push origin v${{ steps.version.outputs.VERSION }}

      - name: Build
        if: steps.tag_check.outputs.EXISTS == 'false'
        run: dotnet build --configuration Release

      - name: Pack
        if: steps.tag_check.outputs.EXISTS == 'false'
        run: |
          dotnet pack ServiceBusConsole.csproj --configuration Release --no-build \
            -p:Version=${{ steps.version.outputs.VERSION }} \
            --output ./artifacts

      # Exchanges the job's GitHub OIDC token for a short-lived (1 hour) nuget.org
      # API key. Requires a trusted publishing policy on nuget.org for
      # zidad/azure-servicebus-console with workflow file "publish.yml".
      - name: NuGet login (OIDC)
        id: nuget_login
        if: steps.tag_check.outputs.EXISTS == 'false'
        uses: NuGet/login@v1
        with:
          user: zidad

      - name: Push to NuGet
        if: steps.tag_check.outputs.EXISTS == 'false'
        run: dotnet nuget push ./artifacts/Net.Azure.ServiceBusConsole.*.nupkg --api-key ${{ steps.nuget_login.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json
